#The provided SQL code queries a Chinook database, focusing on customer information, employee titles, and sales data.
  It extracts details such as customer names and IDs for specific countries, invoices for French customers, and 
  total sales amounts by sales agents. Additionally, it delves into sales statistics for the year 2013 and identifies
  the top-performing sales agent in terms of revenue in 2009.

#1 Which customers are located in the USA?
--Retrieves information about customers located in the USA from the Chinook database.
    SELECT * FROM chinook.customers
      WHERE country = "USA";

#2  Who are the customers in Brazil, and what are their full names, customer IDs, and country?
--Displays the full names, customer IDs, and countries of customers in Brazil from the Chinook database.
    SELECT firstname, lastname, customerID FROM chinook.customers
      WHERE country = "Brazil"; 

#3 What are the invoices for customers from France, including their full names, Invoice IDs, dates of the invoices, and billing countries?
--Fetches invoices for customers from France, including their full names, Invoice IDs, dates, and billing countries.   
    SELECT ci.invoiceid, cc.firstname, cc.lastname, ci.invoicedate, ci.billingcountry
      FROM chinook.customers cc
      LEFT JOIN chinook.invoices ci
      ON cc.customerId = ci.customerId
      WHERE billingcountry = "France";

#4 Who are the employees with the title "Sales Support Agent"?
--Identifies employees with the title "Sales Support Agent" from the Chinook database.
    SELECT * FROM chinook.employees
      WHERE title = "Sales Support Agent";

#5 What is the unique list of billing countries present in the Invoice table?
--Obtains a unique list of billing countries from the Invoice table in the Chinook database.
    SELECT DISTINCT billingcountry FROM chinook.invoices;

#6 What are the invoice total, customer name, country, and sales agent name for all invoices and customers?
--Shows the invoice total, customer name, country, and sales agent name for all invoices and customers in the Chinook database.
    SELECT emp.LastName, emp.Firstname, cust.FirstName, cust.LastName, cust.Country, inv.total
      FROM chinook.employees emp 
      JOIN chinook.Customers cust ON cust.SupportRepId = emp.EmployeeId
      JOIN chinook.Invoices Inv ON Inv.CustomerId = cust.CustomerId;

#7 How many invoices were generated in the year 2013?
--Counts the number of invoices generated in the year 2013 from the Chinook database.
  SELECT COUNT(*) FROM chinook.invoices
    WHERE invoicedate BETWEEN '2013-01-01' AND '2013-12-31'

#8 What is the total sales amount for the year 2013?
--Calculates the total sales amount for the year 2013 from the Chinook database.
  SELECT SUM(TOTAL) FROM chinook.invoices
    WHERE invoicedate BETWEEN '2013-01-01' AND '2013-12-31';

#9  What is the total sales amount for each sales agent with the title "Sales Support Agent"?
--Presents the total sales made by each sales agent with the title "Sales Support Agent" in the Chinook database.
  SELECT emp.FirstName, emp.LastName,
    ROUND(SUM(Inv.Total), 2) as 'Total Sales' 
    FROM chinook.Employees emp

    JOIN chinook.Customers cust 
    ON cust.SupportRepId = emp.EmployeeId
    
    JOIN chinook.Invoices Inv 
    ON Inv.CustomerId = cust.CustomerId
    
    WHERE emp.Title = 'Sales Support Agent' 
    GROUP BY emp.FirstName;

#10 Who is the sales agent that achieved the highest sales in terms of dollars in the year 2009?
--Determines the sales agent who achieved the highest sales in dollars in the year 2009 from the Chinook database.
  SELECT emp.FirstName, emp.LastName,
    ROUND(SUM(Inv.Total), 2) as 'Total Sales' 
    FROM chinook.Employees emp

    JOIN chinook.Customers cust 
    ON cust.SupportRepId = emp.EmployeeId
    
    JOIN chinook.Invoices Inv 
    ON Inv.CustomerId = cust.CustomerId
    
    WHERE emp.Title = 'Sales Support Agent' 
    AND Inv.InvoiceDate LIKE '2009%' 
    GROUP BY emp.FirstName
    ORDER BY (round(sum(Inv.Total), 2))  DESC LIMIT 1;

